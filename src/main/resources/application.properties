# Application
spring.application.name=splitia-backend
server.port=8080

# Database - Neon PostgreSQL
spring.datasource.url=jdbc:postgresql://ep-frosty-frost-a4vt3mjk-pooler.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Yy37LqsWcUXV
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false
spring.flyway.baseline-version=0

# JWT
jwt.secret=your-secret-key-change-in-production-min-256-bits-please-use-a-very-long-secret-key-for-production-environments
jwt.expiration=900000
jwt.refresh-expiration=604800000

# CORS
cors.allowed-origins=http://localhost:3000,http://localhost:3001,https://splitia-next.vercel.app

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Logging
logging.level.com.splitia=INFO
logging.level.org.springframework.security=DEBUG

